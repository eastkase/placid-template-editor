# Database connection (provided by Railway)
DATABASE_URL=postgresql://username:password@host:port/dbname

# Server port
PORT=3001

# Production mode
NODE_ENV=development

# Use API instead of localStorage
VITE_USE_API=false